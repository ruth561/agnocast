こちらのMarkdownは、design-doc.mdに記述された機能を追加するPRをレビューするために
作成されたファイルです。

# 注意点

design-docの内容は複数のPRに分割して実現されます。
そのため、進め方としては、

1. 指定されたPRの範囲を調査し、Markdownにまとめる
2. 自身がまとめたMarkdownをベースにレビューのシナリオを立てる
3. レビューシナリオに出てくる観点ごとにSessionをたて、個別にレビューを行う

レビューは内容ごとに細分化して行います。そのため、それぞれのレビュワーが共有できる
コンテキストのようなもとを作成する必要があります。
